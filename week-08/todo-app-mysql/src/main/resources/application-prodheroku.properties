spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL95Dialect
spring.logging.level.org.hibernate.SQL=info
spring.jpa.show-sql=false

# Previously used one environment variable for each setting. Now using spring.profiles.active, pointing to "application-profilestring.properties"
#
#spring.datasource.url=${APP_DB_PATH}
#spring.datasource.username=${APP_DB_USER}
#spring.datasource.password=${APP_DB_USER_PASSWORD}
#spring.jpa.hibernate.ddl-auto=${APP_HIBERNATE_DDL_AUTO}
#spring.jpa.properties.hibernate.dialect=${APP_HIBERNATE_DIALECT}
#spring.logging.level.org.hibernate.SQL=${APP_HIBERNATE_LOGGING_LEVEL}
#spring.jpa.show-sql=${APP_HIBERNATE_SHOW_SQL}